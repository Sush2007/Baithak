class ClientCache { constructor() { this.cache = new Map(); } set(key, data, ttlMinutes = 5) { this.cache.set(key, { data, expiresAt: Date.now() + ttlMinutes * 60 * 1000 }); } get(key) { const item = this.cache.get(key); if (!item) return null; if (Date.now() > item.expiresAt) { this.cache.delete(key); return null; } return item.data; } } export const feedCache = new ClientCache();